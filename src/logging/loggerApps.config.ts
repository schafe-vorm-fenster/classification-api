export enum ApiClassify {
  classify = "api.classify",
}

export enum ApiScopify {
  scopify = "api.scopify",
}

export enum ClientGoogleGemini {
  config = "client.googlegemini.config",
}

export enum ApiValidation {
  zod = "api.validation.zod-validate",
}
